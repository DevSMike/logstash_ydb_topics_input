input {
  ydb_topics_input {
    # Уникальный идентификатор для этого входа
    id => "my_ydb_input"

    # Количество событий, которые нужно принять из топика
    count => 100

    # Префикс, который будет добавлен к каждому сообщению
    prefix => "Received:"

  }
}

output {

  elasticsearch {
    hosts => ["localhost:9200"]
    index => "my_index"
    document_type => "_doc"
  }
}
